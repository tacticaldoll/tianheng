// Deliberately opens with a `//` comment and no `//!` header, which is exactly the house rule this
// crate's participant reacts to. Adding a header here is not the repair — the fault is the point.

/// A function whose file never says what the file is for.
pub fn helper() -> u32 {
    7
}
