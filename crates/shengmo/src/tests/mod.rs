//! The failure matrices: unit tests beside the judgements they test.
//!
//! Split by topic the way 圭表 and 渾儀 already split theirs, so a matrix sits next to what it
//! tests rather than in a separate binary that must reach back in through `#[path]`.

mod restatement;
