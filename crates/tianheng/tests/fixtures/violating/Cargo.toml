# In-repo fixture: a violating target with one external (registry) dependency.
# `[workspace]` makes this its own workspace root, decoupled from Tianheng's.
[package]
name = "example-core"
version = "0.0.0"
edition = "2021"

[dependencies]
serde = "1"

[workspace]
