## Why

The self-law projection spec elevates one shell reason wording preference into a permanent `SHALL`, but no reaction observes that prose quality and its reaction-preservation scenario cannot fail by construction. Keeping those scenarios presents judgment and type construction as tested guarantees, contrary to the repository rule that a scenario must name an observable reaction.

## What Changes

- Remove the shell-specific architectural-direction requirement and both of its inert scenarios.
- Keep the corrected `tianheng` boundary reason in the constitution and its generated, staleness-checked projection.
- Rely on the existing general forward-reason contract and human adversarial review for reason quality instead of claiming an automated reaction that does not exist.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `self-law-projection`: Remove an unobservable shell-reason wording contract without changing the projected law or its freshness reaction.

## Impact

Touches only the self-law-projection specification and OpenSpec lifecycle artifacts. No Rust code, generated projection, public API, manifest, dependency, package version, or runtime behavior changes.
