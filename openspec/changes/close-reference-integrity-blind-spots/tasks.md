## 1. Corpus and exclusion evidence

- [ ] 1.1 Add controlled TOML and `.gitignore` sources that name an absent tracked-repository path; observe both directions remain green against the current corpus filter.
- [ ] 1.2 Add a controlled `/tests/*.rs` source naming a deleted live script; observe it remain green against the file-wide test exclusion.
- [ ] 1.3 Introduce one inspectable-source predicate, count each admitted file once, remove the file-wide test skip, and make all three directions react while preserving active-plan and record exclusions.

## 2. Live repairs and contract

- [ ] 2.1 Repair every newly visible live stale reference to the current Rust reaction or workflow and confirm the complete reference-integrity target is clean.
- [ ] 2.2 Sync the capability vocabulary from 0/1/2 and stdout/stderr promises to read-only cargo-test pass/fail behavior without weakening fail-loud observation errors.

## 3. Verification and lifecycle

- [ ] 3.1 Run formatting, targeted Clippy, census, the complete repository Definition of Done, and record pre-fix failures plus final commands in PR verification notes.
- [ ] 3.2 Adversarially review corpus completeness, fixture over-exemption, record preservation, spec vocabulary, and compatibility impact.
- [ ] 3.3 Sync the delta spec, complete archive lifecycle, and remove the dated archive before creating the PR.
