# tasks: Hunyi Public-Seam Module Injection Implementation Plan

- [x] Add `module: String` to `PublicSeam::InherentMethod` and `PublicSeam::InherentAssoc` in `crates/hunyi/src/finding.rs`; keep `Display` unchanged (ignore the new field via `..`). <!-- id: 0 -->
- [x] Add `("seam_module", module)` to both variants' `key_fields()` arms, matching the six sibling variants' field name. <!-- id: 1 -->
- [x] Give `inherent_method_seam`/`inherent_assoc_seam` a leading `module: &str` parameter and update all seven call sites in `crates/hunyi/src/collect.rs` (`collect_item_return_impl_traits`, `collect_item_exposures`, `collect_item_dyn_exposures`), each of which already has `module` in scope. <!-- id: 2 -->
- [x] Update `finding.rs`'s `fact_tests` module: `published_seam_fields` and every seam-construction test case for `InherentMethod`/`InherentAssoc` (`every_public_seam_shape_is_named_and_identity_injective` and any sibling case) to carry and assert the new `seam_module` field. <!-- id: 3 -->
- [x] Extend `every_public_seam_shape_is_named_and_identity_injective` (or a new sibling test) with two `InherentMethod` seams sharing the identical `owner`/`name` but differing only in `module`, asserting distinct keys — the general-case injectivity proof that also covers dyn-trait/signature-coupling's shared vocabulary even though neither is integration-reachable today. Do the same for `InherentAssoc`. <!-- id: 4 -->
- [x] Add an integration regression test in `crates/hunyi/src/tests.rs` reproducing the exact two-module scenario via the real `impl_trait_subtree_findings`/`check_impl_trait` path: a shared owner type declared in one module, inherent-impl'd with the same public RPIT method name in two sibling submodules (`plat_unix`/`plat_win`), asserting two distinct findings attributed to their own modules. <!-- id: 5 -->
- [x] Add the adopter-facing `CHANGELOG.md` `[Unreleased]` → `### Fixed` entry with a **BREAKING** marker, naming the false negative closed, the reachable capability (impl-trait subtree), the structurally-shared-but-not-yet-reachable siblings (dyn-trait, signature-coupling), and the baseline-staleness consequence — matching the `governing_package` entry's own wording shape. <!-- id: 6 -->
- [x] Update `openspec/specs/semantic-signature-coupling/spec.md`'s seam-qualification requirement (and `semantic-dyn-trait-boundary`/`semantic-impl-trait-boundary` if either states or implies inherent-method injectivity independent of signature-coupling) to state the module-qualified guarantee for inherent method/assoc seams; grep each touched spec's whole file for any stale prose that already (incorrectly) claimed or denied this guarantee. <!-- id: 7 -->
- [x] Update `BACKLOG.md`: close the `InherentMethod`/`InherentAssoc` DESIGN-BREAKING entry, referencing this change, matching the style of prior closed entries. Leave the sibling `InherentGenerics` entry untouched. <!-- id: 8 -->
- [x] Run the full Definition of Done from the workspace root (build, four clippy passes, fmt, `TIANHENG_WORKSPACE_TESTS=1` test, doc, `cargo deny`, both release-coherence scripts, `test_examples.sh`) and report actual output. <!-- id: 9 -->
