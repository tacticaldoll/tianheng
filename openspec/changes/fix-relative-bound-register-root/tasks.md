## 1. Regression Evidence

- [x] 1.1 Add a failure-matrix case that invokes the bound-register gate from a fixture repository's parent using a relative repository path and reaches projection access.
- [x] 1.2 Run the focused matrix before the fix and record the observed relative-root failure for the PR verification evidence.

## 2. Stable Repository Root

- [x] 2.1 Resolve the repository argument to one physical absolute root before any observation while preserving the later checked directory transition.
- [x] 2.2 Run the focused bound-register failure matrix and positive gate against the repaired implementation.

## 3. Lifecycle And Verification

- [x] 3.1 Run the repository Definition of Done and record the commands and results for the PR.
- [ ] 3.2 Sync the delta requirement into the main observation-bound-register spec and remove the completed OpenSpec archive copy while retaining the archive placeholder.
