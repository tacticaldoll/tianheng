## 1. Documentation & Cookbook Recipe

- [x] 1.1 Add Three-Layer Agent Law recipe to `COOKBOOK.md` covering preamble discipline, `GovernanceTest::for_constitution`, `.assert_projection_fresh_with_preamble()`, and `BLESS=1` workflow.
- [x] 1.2 Update `BACKLOG.md` live decision index to reflect promotion of the Three-Layer Agent Law item to BUILT/HISTORY.

## 2. Verification

- [x] 2.1 Verify doctests, rustdoc, and workspace checks (`cargo test`, `cargo fmt --check`, `scripts/check_release_coherence.sh`).
