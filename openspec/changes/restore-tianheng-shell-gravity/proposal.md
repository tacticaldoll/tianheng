## Why

The Tianheng shell boundary now repeats its dependency allowlist as prose, so its projected reason
adds no architectural direction. The rule is correct; its Qiányí gravity needs restoration without
changing reaction semantics.

## What Changes

- Replace only the shell boundary reason with forward, layer-oriented language bounded to direct
  normal dependency observation.
- Regenerate the self-law projection and prove the rule identity and outcomes are unchanged.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `self-law-projection`: The shell boundary projection carries architectural direction rather than
  duplicating the rendered allowlist.

## Impact

The change affects only the steward-owned self-constitution, its generated projection, and the
canonical self-law projection requirement. It changes no boundary target, rule, parameter, severity,
baseline, dependency, public API, package version, or adopter behavior.
