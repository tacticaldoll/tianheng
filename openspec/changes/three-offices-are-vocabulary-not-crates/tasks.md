# Tasks

- [ ] `PROJECT.md`: replace *crate-or-convention as their nature dictates* with the located referents and the
      boundary argument. Record that the question was asked three times and why it is closed, so it is not
      re-minted.
- [ ] `crates/tianheng/tests/self_governance.rs`: the preamble line names each surface by **what it does**
      rather than as a set.
- [ ] `AGENTS.self-law.md`: regenerate; never hand-edit. `self_law_projection_is_fresh` must hold.
- [ ] `openspec/specs/self-law-projection/spec.md`: the new requirement, with its lack of a reaction stated
      beside it.
- [ ] Sweep the character 司 across the whole file, not the diff: if the set name goes, every contrast that
      leans on it needs an antecedent.
- [ ] Full `AGENTS.md` Definition of Done.
