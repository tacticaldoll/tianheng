## 1. Preserve and challenge the accepted law

- [ ] 1.1 Record the focused self-governance test passing with the accepted boundary and current manifest edge.
- [ ] 1.2 Remove `xingbiao` from only the candidate shell allowlist and reason, then record the focused reaction rejecting the still-present edge.

## 2. Migrate the product toward the tightened reason

- [ ] 2.1 Remove the unused direct normal `xingbiao` dependency from `crates/tianheng/Cargo.toml` and align adjacent shell-dependency comments.
- [ ] 2.2 Record the focused self-governance test passing after migration.
- [ ] 2.3 Temporarily add an independent forbidden direct normal dependency, record the focused reaction rejecting it, then remove the witness.

## 3. Refresh and verify the projection

- [ ] 3.1 Regenerate `AGENTS.self-law.md` through the blessed projection test and confirm only the intended shell boundary changes.
- [ ] 3.2 Run the unblessed self-law freshness test and the repository Definition of Done.
- [ ] 3.3 Review the complete diff for amendment scope, baseline stability, compatibility, and absence of retired shell-allowlist prose.

## 4. Synchronize the accepted specification

- [ ] 4.1 Sync the delta requirement into `openspec/specs/self-law-projection/spec.md`.
- [ ] 4.2 Archive and prune the completed change scaffolding while retaining `openspec/changes/archive/.gitkeep`.
