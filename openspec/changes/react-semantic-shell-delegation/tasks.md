## 1. Source-Shape Reaction

- [ ] 1.1 Generalize the existing brace-counted `bounds_body` helper into one shared named-function body recognizer without weakening the observer-bounds reaction.
- [ ] 1.2 Add a shell delegation test that requires one direct `hunyi::check_all(constitution.semantic_boundaries(), manifest_path)` access inside `evaluate_constitution`.
- [ ] 1.3 Reintroduce a shell-local semantic emptiness guard temporarily, run the focused test to observe failure, then restore the delegated production source.
- [ ] 1.4 Run the focused observer-protocol test target against the restored source.

## 2. Verification And Lifecycle

- [ ] 2.1 Run the repository Definition of Done and the post-change self-governance reaction.
- [ ] 2.2 Sync the modified observer-protocol requirement and archive the completed change without retaining a dated archive copy.
