## 1. Dimension-Owned Identity Catalogs

- [x] 1.1 Add exhaustive 圭表 crate/module fact and dependency-kind catalogs that pin exact keys
- [x] 1.2 Add exhaustive 渾儀 semantic fact and finite nested seam/discriminator catalogs that pin every identity shape and each key-producing canonical value
- [x] 1.3 Add an exhaustive all-features 漏刻 runtime fact catalog that includes production and CI-audit facts
- [x] 1.4 Pin representative production-emitted target/rule/key wiring once per dimension without duplicating every capability fixture

## 2. Wire Documentation

- [x] 2.1 Mark async signature-tail and any other cataloged key canonicalizers as version-2 wire-sensitive without freezing human presentation
- [x] 2.2 Update the post-0.2 backlog entry to record the completed compatibility reaction and retain the breaking identity questions separately

## 3. Adversarial Verification

- [x] 3.1 Prove each catalog is variant-exhaustive and rejects an intentional representative namespace/code/field/value drift
- [x] 3.2 Review the implementation for accidental presentation snapshots, production vocabulary duplication, or cross-dimension dependency edges and resolve findings one at a time
- [x] 3.3 Run the repository Definition of Done and confirm package versions and manifests are unchanged
