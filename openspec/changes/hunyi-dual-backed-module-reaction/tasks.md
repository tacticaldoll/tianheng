# tasks: Hunyi Dual-Backed Module Anchor Reaction Implementation Plan

- [ ] Introduce the crate-internal `ModuleFile { Absent, One, Ambiguous { flat, nested } }` in `crates/hunyi/src/module_resolve.rs` and return it from `locate_module_file`, keeping the lookup a pure filesystem question that names no module path. <!-- id: 0 -->
- [ ] Add the dual-backed constitution-error builder to `crates/hunyi/src/errors.rs`, naming both resolved paths and the exactly-one-file rule, and claiming no parity with either sibling's wording — 圭表's and 漏刻's existing messages already differ from each other three ways, so a "parallel twin" doc claim would pick a side while sounding like agreement. <!-- id: 1 -->
- [ ] React on `Ambiguous` in `module_resolve.rs::descend` (the single-module anchor path behind signature-coupling, visibility, dyn/impl-trait, and async-exposure), ensuring it bypasses the existing `#[cfg]` absence-tolerance rather than being skipped by it. <!-- id: 2 -->
- [ ] React on `Ambiguous` in `scan.rs::resolve_child_modules` (the crate-wide walk behind trait-impl locality, unsafe confinement, and forbidden marker), with the same cfg-bypass, so the two walkers do not drift apart on this shape as they did in 0.2.2. <!-- id: 3 -->
- [ ] Add unit coverage in `crates/hunyi/src/tests.rs` for all four `locate_module_file` states through both call sites, including the motivating false negative (a forbidden exposure written only in `child/mod.rs` must not pass) and a `#[cfg]`-gated dual-backed declaration still erroring. <!-- id: 4 -->
- [ ] Extend `crates/tianheng/tests/dual_backed_module_conformance.rs` from two dimensions to three, exhaustively over the four states, asserting all three dimensions refuse to judge with exit 2 on the ambiguity — pinning exit codes, not error wording. <!-- id: 5 -->
- [ ] Add the adopter-facing `CHANGELOG.md` `[Unreleased]` → `### Fixed` entry, naming the standalone-渾儀 reach of the closed false negative and stating both trigger shapes honestly: a live E0761 declaration (uncompilable) and a `#[cfg]`-gated-off declaration with both files present (which compiles, and newly stops being judged). <!-- id: 6 -->
- [ ] Run the full Definition of Done from the workspace root (build, four clippy passes including `--workspace` and `-p louke`, fmt, `TIANHENG_WORKSPACE_TESTS=1` test, doc, `cargo deny`, both release-coherence scripts, `test_examples.sh`) and report actual output; report rather than quietly fix any fixture or example that turns out to be dual-backed. <!-- id: 7 -->
