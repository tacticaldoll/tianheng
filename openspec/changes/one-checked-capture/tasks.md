## 1. The eleventh property

- [ ] 1.1 Add the recognizer: executed lines containing `< <(`, permitted when the producer's first word is a
      builtin over memory (`printf`, `echo`). Read through the executed-text view, never the whole file.
- [ ] 1.2 Add the `PROPERTIES` entry, subject `Gate`, beside the backstop entries where the other exit-contract
      properties sit.
- [ ] 1.3 Confirm the surface reads 6 of 6 on arrival — the migration landed first for exactly this reason
      (design D4). If any gate fails, the property must not land until it passes.
- [ ] 1.4 Re-bless the projection; the table gains a column and the printed figure moves ten to eleven by
      measurement.

## 2. The bound narrows

- [ ] 2.1 Narrow the scenario body: the process-substitution shape is now observed; what remains is a command
      substitution whose status nobody inspects, and a pipeline whose non-final stage fails.
- [ ] 2.2 **Do not touch the heading.** Verify after sync that the derived id is unchanged, that the `PINNED-BY`
      still resolves, and that the typed declaration still matches — the bijection is what proves it.
- [ ] 2.3 Narrow the rationale in `tianheng::observation_bounds()` to match, and re-bless
      `docs/observation-bound-extents.md`. The register's figures must **not** move: no bound is added or removed.

## 3. Observed failing

- [ ] 3.1 A fixture gate using `done < <(git ls-files)` is refused, naming it. The other ten properties must be
      held by that fixture so the refusal is about this one.
- [ ] 3.2 A fixture using `done < <(printf '%s\n' "$held")` is accepted.
- [ ] 3.3 The per-property test covers it with no edit — it iterates `PROPERTIES`, so the eleventh must enter the
      fixture builder's withholding by label. If it needs a change, the array is not the single declaration site it
      claims to be.
- [ ] 3.4 Perturb the recognizer to always hold and confirm the per-property test fails.
- [ ] 3.5 Re-run the two migration falsifiers to confirm the property and the migration agree: both gates still
      exit 2 on a truncated read.

## 4. Verification

- [ ] 4.1 Every observation in the pull request's `## Verification`.
- [ ] 4.2 Full Definition of Done, then again from a clean clone.
- [ ] 4.3 `openspec validate` strict before and after sync; archive pruned.
