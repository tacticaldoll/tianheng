# Tasks

## 1. The bound's stated cause

- [x] 1.1 Narrow the THEN clause, heading untouched, per this requirement's own rule that a bound's heading is
      its id.
- [x] 1.2 Confirm the claim the correction rests on: `gate-shape-contract`'s `exit codes` property requires an
      exact code, and its remedy already cites this instance.
- [x] 1.3 Regenerate both bound projections; the register's count is unchanged.

## 2. BACKLOG registrations

- [x] 2.1 Record the `1-versus-2` re-evaluation: one instance (`86e8592`), both directions, its enabler closed,
      and a trigger the old text could not distinguish.
- [x] 2.2 File the class this window kept finding — every normative SHALL either has a reaction or is a declared
      bound — with a **measured** cost, not an estimated one.

## 3. Definition of Done

- [x] 3.1 The full Definition of Done in `AGENTS.md` clean.
- [x] 3.2 `CHANGELOG.md` `[Unreleased]` records the narrowing — no version bump.
- [ ] 3.3 Sync the delta and prune the dated archive copy.
