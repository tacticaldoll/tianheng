# Tasks

- [x] 1 The participant declares a second computed bound, `Reached::OverReacts`, for a header below a leading
      comment.
- [x] 2 A test pins it: the license-header file reacts (the bound), and the same content with its header on line
      one does not (so the pin cannot hold for the wrong reason).
- [x] 3 The bounds test stops asserting a bare count and asserts the declared set, so a bound added without a pin
      is visible.
- [x] 4 The README says the example demonstrates two extents.
- [x] 5 Full Definition of Done clean, `CHANGELOG.md` records it, no version bump.
- [ ] 6 Sync the delta and prune the dated archive copy.
