## MODIFIED Requirements

### Requirement: Every generated document and the reaction holding it fresh SHALL correspond, in both directions

Each enumerated document SHALL name the Rust unit that generates it. The correspondence is counted per
blessing call site in the tests of the members holding this repository's governance, which is where those call
sites live once the apparatus is outside every published package — the law's own member for the projection of
the law, and the collation member for the registers it produces.

The requirement names a location because the count is over call sites, and a count over a directory that no
longer holds them would report zero and read as a register with nothing left to hold.

A document's header SHALL name the unit holding it **fresh**, not the code it renders. Those differ once a
law is library code and the reaction running it is a test: the register keeps every tracked path a header
names and reports two as ambiguous rather than choosing, so a header naming both is refused. The law is
therefore named by module rather than by file where a reader still needs to find it.

#### Scenario: A reaction holds a projection no document registers
- **WHEN** a Rust test unit holds a projection fresh and no enumerated document names it
- **THEN** the reaction fails, naming the unit and the path it blesses
