## 1. Markdown projection

- [ ] 1.1 Classify `anchor` as structural metadata and render it as a standalone Markdown element.
- [ ] 1.2 Replace the leaking-parameter assertion with focused presence, absence, and separation
  regression coverage.

## 2. Diff hygiene

- [ ] 2.1 Remove the reported trailing blank EOF lines from `PROJECT.md`, `adopter-surface`, and
  `semantic-impl-trait-boundary`.
- [ ] 2.2 Verify `git diff --check v0.2.3...HEAD` is clean.

## 3. Verification and lifecycle

- [ ] 3.1 Run the complete repository Definition of Done without changing package versions.
- [ ] 3.2 Sync the constitution-projection delta into the main spec and retire the completed
  OpenSpec scaffold without retaining a dated archive copy.
