## Why

The ANSI presentation module repeats Tianheng's current shell dependency allowlist in a hand-written
comment. That copied census has already drifted from the enforced constitution, so the comment should
retain only the architectural reason and point readers to the generated self-law projection.

## What Changes

- Remove the copied dependency members from `term_color.rs`.
- Keep the useful explanation that an added colour crate would violate the shell's dependency self-law.
- Add a repository-only self-governance reaction that rejects a copied `restrict_dependencies_to(`
  declaration shape in authored Tianheng shell source.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `self-law-projection`: Extend the single-source contract to keep authored shell source from restating
  the dependency declaration owned by the constitution and generated projection.

## Impact

The focused self-governance test, `crates/tianheng/src/runner/term_color.rs`, the synchronized
`self-law-projection` specification, and lifecycle artifacts are affected. Public APIs, runtime behavior,
dependencies, manifests, package versions, baselines, and the enforced constitution remain unchanged.
