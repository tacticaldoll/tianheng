## MODIFIED Requirements

### Requirement: The preamble describes only how to read the projection, not crate-specific law

The artifact's fixed preamble SHALL describe only how to read and use the projection and the reaction loop it serves (declare intent in code; observe only what has an observation source; react with the 0/1/2 outcomes; repair toward the declared `reason`; never weaken the law to pass; 三儀 measure, and the governance surfaces are named where they live rather than as a set). The preamble SHALL NOT make crate-specific architectural claims; any such claim SHALL appear only in the generated projection, where it traces to a boundary that actually reacts (no open-loop prose prescription).

#### Scenario: Crate-specific law appears only in the generated projection

- **WHEN** the preamble is read
- **THEN** it describes the reaction loop and how to read the projection, and makes no crate-specific architectural claim — such claims appear only in the generated boundary projection below it

The preamble SHALL NOT name a governance office that has no reaction. One did — 校讎, for the amendment flow —
and the same paragraph of `PROJECT.md` that introduced it forbids naming before the reaction exists. A handle
with no referent in the preamble is worse than absent: it is loaded into every agent's context as though it
named something to be found.
