# Static-review remediation — `v0.4.0..release/0.5.0`

A defect queue, not a report: every unchecked box here is work the project still owes itself.
Check the box in the same commit that closes it — this file is the durable, committed fallback
tracker for the campaign, independent of any in-session TodoWrite state.

## Provenance

Produced by a full gate-based static review (fornax `static-review` skill) plus an adversarial
contract audit over the whole `v0.4.0..HEAD` range (265 commits, 236 files): 145 code files
individually triaged (0 Red, 6 Yellow, 139 Green), the 6 Yellow files taken through the full 8-gate
ladder, and 126 contract clauses + 181 CHANGELOG/BACKLOG claims adversarially verified against
`HEAD` (falsifiers constructed live where feasible, not just read for plausibility). Full findings
detail lives in this campaign's conversation record; this file is the input to the remaining
lifecycle, not a substitute for it.

Ordering: Phase 1 (release-blocking) → Phase 2 (should-fix) → Phase 3 (documentation
reconciliation) → partial-claims batch. Within a phase, gate/contract-violation order, then
file:line.

## Phase 3 — documentation reconciliation (9)

**Closed as a batch, PR #569 (`8b68e00`), merged into `release/0.5.0`.** All nine traced to the
same root cause: `64ed18c` (shell-to-Rust migration) and `5abda1f` (`gate-shape-contract`
retirement) each correctly described what they removed, but left CHANGELOG.md/BACKLOG.md entries
elsewhere in the same window narrating the deleted mechanism as current. `AGENTS.md`'s
vocabulary-sweep rule was widened to name capability retirement, not only renaming, as requiring
the same sweep.

- [x] observation-bounds.md content-assertion test claim — CHANGELOG.md Self-governance entry added
- [x] block-comment delimiter stop / 12-line cap claim — same entry
- [x] second "third floor" (block-comment citation) claim — same entry
- [x] gate-shape-contract 1-versus-2 bound three-site-correction claim — same entry
- [x] gate-shape-contract process-substitution property claim — same entry
- [x] openspec validate wired into CI/DoD claim — CHANGELOG.md corrected directly; BACKLOG.md gains
      a new READY-PATCH entry tracking the still-open gap
- [x] gate-shape-contract capability ("closes a gate's shape") claim — BACKLOG.md entry annotated
      with its later retirement
- [x] swallowed-subshell-status eleventh-property claim — BACKLOG.md entry annotated
- [x] reference_integrity.rs companion-failure-matrix claim — BACKLOG.md entry reopened as
      READY-PATCH (prior CLOSED claim named the wrong subject)

## Phase 1 — blocking before 0.5.0 ships (4)

- [x] **Gate 3 error swallowing before an irreversible `cargo publish`**
      `crates/kanhe/src/publish_source_gate.rs`, `workspace_version` · Gate 3 FAIL
      Closed via PR #570 (`de47967`).
- [x] **Contract-violated #9: `dyn Observer` scan blind spot**
      `crates/kanhe/tests/observer_protocol.rs`'s `composition_introduces_no_trait_object` scanned the
      wrong crate's `src/` (`CARGO_MANIFEST_DIR` resolved to `crates/kanhe/src`, not the crate that
      actually composes `Observer`). Fixed to resolve the workspace root and scan
      `crates/tianheng/src` explicitly.
- [x] **Contract-violated #1: CLI `list`/`check` flag-rejection set not derived**
      `crates/tianheng/src/runner.rs`'s `dispatch_list` — was a hand-written array, not derived from
      `ParsedArgs`; a new check-only flag silently passed `list` instead of being refused. Fixed by
      exhaustively destructuring `ParsedArgs` (no `..`), so a new field not handled here fails to
      compile.
- [x] **Contract-violated #11: `merge-pr.sh`'s `-d` short flag lacks the consequence-naming message**
      `scripts/merge-pr.sh` (`--delete-branch` arm) — `-d` was refused but fell through to the
      generic message instead of naming branch-deletion/auto-close. Fixed by sharing the arm; pinned
      by a new assertion in `merge_workflow.rs`.

**Phase 1 complete — all 4 release-blocking items closed.**

## Phase 2 — should fix (10)

- [x] **Gate 4: leftover scaffolding braces** — `crates/kanhe/tests/bound_register.rs`'s
      `every_pinning_citation_resolves_to_one_registered_test`. Fixed (pure dedent, confirmed by a
      whitespace-only diff; no behavior change).
- [x] **Gate 7: duplicated `git()` wrapper** — `crates/kanhe/src/release_coherence_gate.rs` /
      `crates/kanhe/src/publish_source_gate.rs`. Fixed: both now delegate to a new
      `hermetic_git::run(repo, flags, args)`.
- [x] **Contract #4: duplicate `PINNED-BY` definition names only a count, not both sites** —
      `crates/kanhe/tests/bound_register.rs`'s `every_pinning_citation_resolves_to_one_registered_test`.
      Fixed: extracted `definition_count_offence`, now lists every definition site under the count.
- [x] **Contract #5: mutation-survival message doesn't name the bound it defends** —
      `crates/kanhe/tests/pin_bites.rs`. Fixed: `cited_names()` replaced with `cited_bounds()`, mapping
      each citation to its bound id(s) via the canonical parser; survival message now names the
      citation, the mutation, and the bound.
- [x] **Contract #6: bound declared in prose outside a scenario is never scanned** —
      `crates/kanhe/src/bound_register_parse.rs`. Fixed via a full OpenSpec change
      (`restore-undeclared-prose-bound-scan`): ported the shell era's `BOUND_PROSE` scan; running it against
      the live corpus also found and fixed one stale "stated bound" claim in
      `runtime-origin-assertion/spec.md` that no longer reflected actual scanner behavior.
- [x] **Contract #7: projection has only byte-diff freshness, no content assertion** —
      `crates/kanhe/tests/bound_register.rs`'s `the_register_projection_is_generated_and_fresh`. Fixed:
      added `the_projection_s_disclosures_are_asserted_not_only_its_freshness`, reading the blessed document
      directly and refusing a rendered backslash.
- [x] **Contract #8: census only checks the first figure-match per line** —
      `crates/kanhe/src/census.rs`'s `figures_in`. Fixed: now collects every occurrence; fixing it surfaced
      and closed a second overlap defect (digit-run and compound-word suffixes read as spurious extra
      matches).
- [x] **Contract #10: `git check-ignore` reference check depends on directory existence** —
      `reference_integrity`'s `ignored` helper. Fixed: a directory-only ignore pattern only matches a
      candidate git can `lstat` as a directory, so a generated directory not yet built locally read as
      "not ignored." Now also queries with a trailing slash forced on, which reads as a directory
      regardless of what is on disk.
- [x] **Contract #2: test name doesn't match what it exercises** —
      `external_confinement`'s `confine_external_crate_conflates_coincident_lib_and_bin_conventional_paths`.
      Investigated: its fixture declared no `bin` target at all and was byte-for-byte the same case as an
      existing test above it, so the name's claimed hazard was never exercised. Confirmed via a real lib+bin
      fixture that `check_module_boundary`'s per-root evaluation already handles the coincident-path case
      correctly (each unit's own copy of a leak is reported once, for its own unit, neither merged nor
      dropped). Deleted the redundant test and added one that pins the real, previously-unpinned behavior.
- [x] **Contract #3: DoD focused-matrix ordering/contiguity check was never rebuilt in Rust** —
      `dod_coherence`. Investigated and retired rather than rebuilt: the deleted shell predecessor's
      contiguity/non-recursion check governed three separate "focused example matrix" scripts and a
      driver script, none of which exist anymore — the shell-to-Rust migration consolidated them into
      one Rust test (`examples_suite`) that owns its own ordering internally and is named on a single
      DoD/CI line the existing membership check already covers. Recorded the decision in the test file's
      own doc comment so the question is not silently rediscovered.

## Partial claims — batch spot-check (20)

Evidence exists for each but is incomplete relative to what the claim asserts (usually: the code
change is real, but the claimed permanent verification artifact was actually a manual/temporary
probe). Lower priority than the above; verify in one batch pass rather than opening 20 individual
changes.

- [x] Bounds-method anchor-line reader — table-driven test covering every input shape. Verified: not
      literally table-driven, but `observer_protocol.rs` already covers every input shape by hand
      (absent, mid-line mention, ambiguous/decoy, mentioned-in-comment, single valid) — no coverage gap,
      only a style mismatch with the original claim's wording.
- [x] Clean-register-run census figures — "five prose figures now number-free" not fully traced. Traced to
      the deleted shell-era bound-register gate script, which printed all five
      figures to stdout on a clean run. That stdout print did not survive the port (`bound_register.rs` has
      none) and is low-value under `cargo test`'s default output capture (no DoD/CI command uses
      `--nocapture`). The substantive guarantee survived by a different, arguably better route: the one
      figure that must stay in prose (bounds-across-capabilities) is reacted to by `census.rs`, and no
      current tracked document hand-writes any of the other four — `docs/observation-bounds.md`'s generated
      header itself now surfaces bound and unpinned counts durably. No live staleness risk found; recorded
      rather than reimplementing a stdout print nothing would see.
- [x] `r#name` citation handling — the "reports required line shape" half (same root cause as
      Contract #4 above). Added `a_raw_identifier_citation_resolves_to_its_definition`, extracting the
      shared `definition_pattern` so the test and the real check cannot diverge. `#` carries no special
      meaning to POSIX ERE, so this was already correct by construction — the gap was in coverage, not
      behavior.
- [x] Bound register `git ls-files` enumeration refusal + census grep exit>1 check. Added
      `tracked_specs_refuses_a_repository_with_no_spec_md_rather_than_reporting_it_empty` (the ls-files
      enumeration refusal) and `search_and_must_panic_on_a_genuine_failure_not_only_on_a_clean_miss` (the
      exit>1 distinction, shared by every `search`/`must` call site including census-adjacent parsing).
- [x] louke audit-scoped bounds gating — "tianheng shell unaffected" half. Verified directly:
      `crates/tianheng/Cargo.toml` declares `louke = { workspace = true, features = ["audit"] }`, so the
      composed shell always builds louke with `audit` on and the feature-gating fix changes nothing for it.
      `observation_bound_model.rs`'s bijection (run with `--all-features` per the fix's own design) is
      currently green, confirming the gating still holds.
- [x] Bound register `ERR`-trap exit contract — survival into the Rust port unconfirmed. Same fix as the
      `git ls-files` item above: `search_and_must_panic_on_a_genuine_failure_not_only_on_a_clean_miss` also
      covers an unavailable target repository (the shell era's `cd` failure, measured to fail at spawn
      rather than return a status code) panicking rather than reading as a clean miss.
- [x] Three text recognizers reading past their region — third instance not located. Located:
      `source_regions.rs`'s `prose_keeps_visible_text_beside_an_html_comment`, asserting both directions
      together (visible text beside an HTML comment is seen; text inside the comment span is not) — a
      real, permanent, currently-passing regression test for the projection register's prose reader, closing
      the one direction the original fix's commit did not negative-verify against the unrepaired code.
- [x] reference-integrity governance-document set — zero-corpus/fixture-only argument half. Traced to
      `fix(governance): make reference policy hermetic` (`63910df`) — a purely shell-era fix (only
      the deleted shell-era reference-integrity gate script and its test twin) closing an
      ambient-environment-variable override of the governance-document list. The Rust port's
      `GOVERNANCE_DOCUMENTS` is a compile-time `const` array, never read from an environment variable at
      all — the override class this fix hardened against is structurally impossible in the port, so there
      is nothing to carry forward; the concern is moot by architecture rather than unaddressed.
- [x] Bound register cannot-judge on unavailable target repo — survival into Rust port unconfirmed. Same
      fix as the `ERR`-trap item above — one fixture, both claims.
- [x] Lexical trait-object guard recursion — "wrapped-signature continuation line" sub-claim. Located and
      confirmed currently green: `observer_protocol.rs`'s `a_trait_object_on_a_continuation_line_is_not_recognized`
      feeds the recognizer both the one-line control (recognized) and the wrapped form split across the
      `pub fn` line and its continuation (not recognized) — exactly the claim, and currently passing.
- [x] Dimension enumerator `publish` array fix — four-state verification was manual, not committed. Extracted
      `excluded_by_publish_field` from `dimension_crates()` in `shengmo/tests/self_governance.rs` and added
      `the_publish_field_s_four_states_are_read_correctly`, asserting all four cargo-reported shapes (absent,
      `false`, a private-registry list, and — via reverting to "every array excludes" — the specific false
      negative the fix closed). Verified both negative directions: the historical "every array excludes"
      bug fails the new test on the private-registry case, and the correct "only the empty array excludes"
      passes all four.
- [x] Dimension enumerator `cargo metadata` identity fix — alias-defeat verification was manual. Verified by
      direct reasoning rather than a new fixture: `dimension_crates()` reads `package["name"]` straight from
      `cargo metadata`'s own JSON, which cargo always resolves to the dependency's real crate name (the
      alias lives only in the separate `rename` field) — the manifest-text-matching bug class this fix
      closed cannot recur here by construction, since there is no text to alias against once the read comes
      from cargo's own resolved identity rather than from a source line.
- [x] `publish.sh` allowlist conversion — "controlled cargo stand-in" verification was manual. Verified as
      already permanent, not manual: `publish_workflow.rs`'s `only_an_allowlisted_argument_reaches_the_publish`
      exercises exactly the dangerous flags the CHANGELOG names (`--exclude`, `--no-verify`, `--allow-dirty`,
      `--config`) against a real fake-cargo stand-in built by `write_executable`/`run_wrapper`, and is
      currently green — the manual verification described did land as a committed regression test.
- [x] `cargo package --list -p tianheng` target-count drop — self-law projection diff not recounted.
      Recounted at the actual commit (`09c29d0`): the CHANGELOG's "differs by exactly three lines" claim
      undercounted — the real diff is 19 insertions / 3 deletions, since two whole new crate sections
      (`shengmo`, and this repository's own split-tally crate) were added alongside the 3-line preamble
      edit. Corrected the CHANGELOG to describe the diff accurately: 3 pre-existing lines changed (the
      preamble), the rest additive for the two new members.
- [x] Four governance documents restating the dependency allowlist — enumeration not recounted. Verified
      by the durability test rather than a historical recount: `law_restatement.rs`'s
      `no_governance_document_restates_a_declared_allowlist` is a live, currently-passing reaction that
      re-checks every tracked governance document against every declared allowlist on every run — it is
      green today, meaning zero restatements exist in the current tree, which confirms the fix held rather
      than merely that four were found once.
- [x] `PROJECT.md` retired-term sweep — finer per-file claim not itemized. The requirement itself
      (`self-law-projection/spec.md`'s "The preamble SHALL say what each governance name does") discloses
      its own unobserved status by design — a judgement over prose, measured and rejected as automatable
      three times, stated rather than left for a reader to discover. Verified what IS checkable: each of the
      five cited referent files (`guibiao::projection`, `tianheng::runner::render`, `xuanji::baseline`,
      `.github/CODEOWNERS`, `tianheng::constitution`) exists and matches its claimed role. A full-repository
      grep for all three retired terms found every occurrence consistent with the CHANGELOG's own
      enumeration — no publishable crate's `///`/`//!` documentation carries them outside the one named `//`
      exception.
- [x] New `### Self-governance` CHANGELOG heading — "twenty entries previously misfiled" not recounted.
      Traced through history: the figure already went through its own adversarial-review correction cycle
      before this audit — commit `155f6a3` ("stop miscounting my own populations") found the original
      "nineteen entries, ten in `[Unreleased]`" wrong by exactly one (a directory-matching fix had enlarged
      the population it counted) and corrected all four sites stating it to twenty/eleven. No later commit
      touches the figure. Verified rather than recounted from scratch, since a fresh count would only repeat
      a check this repository's own process already ran.
- [x] `scripts/` line-count claim (6,827 vs. measured 6,841). Recounted at the exact pre-migration commit
      (`64ed18c^`) against the sentence's own stated scope — seven `check_*.sh` gates, eleven `test_*.sh`
      twins, seven `lib/*.sh` libraries, `.sh` files only: **6,827, confirmed exact.** The alternative 6,841
      figure adds a mutation-fixture data table the deleted shell libraries directory once held (14 lines,
      not a shell library the sentence's own wording covers) — 6,827 + 14 = 6,841 exactly. No error in the
      CHANGELOG;
      the two figures answer different, both-legitimate questions about what counts as "libraries."
- [x] Publish wrapper `--manifest-path` refusal ordering — not re-run end-to-end. Same evidence as the
      allowlist-conversion item above: `only_an_allowlisted_argument_reaches_the_publish` includes both
      `--manifest-path` spellings among the refused arguments, run end-to-end against the fake-cargo
      stand-in (this file's own doc comment states it exists because "nothing ran the script to find out"
      previously) — currently green, so the refusal is confirmed live, not merely argued.
- [x] `whitespace_hygiene`/`dod_coherence` failure matrices — specific fixtures not both located.
      `whitespace_hygiene.rs`'s claimed fixture is present and correct
      (`an_unreadable_tracked_file_is_refused_rather_than_skipped`). `dod_coherence.rs`'s claimed
      "zero-commands direction" fixture was genuinely missing — the production guard exists
      (`dod_commands`'s `assert!(!commands.is_empty(), ...)`) but no test exercised it. Added
      `a_dod_block_with_no_commands_is_refused_not_reported_clean`. Verified both negative directions:
      removing the guard makes the new test fail, and the guard's restoration passes it again.
